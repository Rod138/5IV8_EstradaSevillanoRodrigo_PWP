<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Registro de Estudiantes</title>
    <link rel="stylesheet" href="/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined" rel="stylesheet" />
</head>
<body>
    <header>
        <h1>Registro de Estudiantes</h1>
    </header>
    <br>
    <form action="/estudiantes" method="POST">
        <label for="nombre">Nombre</label>
        <input type="text" id="nombre" name="nombre" placeholder="Nombre del estudiante" required>
        <label for="edad">Edad</label>
        <input type="number" id="edad" name="edad" placeholder="Edad del estudiante" required>
        <label for="curso">Curso</label>
        <input type="text" id="curso" name="curso" placeholder="Curso del estudiante" required>
        <button type="submit">Registrar Estudiante</button>
    </form>

     <ul>
        <% estudiantes.forEach(function(estudiante){ %>
            <li><strong><%= estudiante.nombre %></strong> (Edad: <%= estudiante.edad %>, Curso: <%= estudiante.curso %>)
            <a href="/estudiantes/edit/<%= estudiante.id %>">Editar</a>
            <a href="/estudiantes/delete/<%= estudiante.id %>">Eliminar</a>
            </li>
        <% }); %>
    </ul>
</body>
</html>
