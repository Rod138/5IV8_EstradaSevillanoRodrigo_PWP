<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cursos del Círculo Infernal</title>
</head>

<body>
    <main class="container">
        <table class="table" border="1">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Nombre</th>
                    <th>Descripción</th>
                    <th>Docente</th>
                </tr>
            </thead>
            <% if(Array.isArray(cursos) && cursos.length> 0) {
                %>
                <tbody>
                    <tr>
                        <% cursos.forEach(function(curso){ %>
                            <td><%= curso.id %></td>
                            <td><%= curso.nombre || 'No disponible'%></td>
                            <td><%= curso.descripcion || 'No disponible'%></td>
                            <td><%= curso.docente || 'No asignado'%></td>
                    </tr>
                    <% }); %>
                </tbody>
        </table>
        <% } else { %>
            <div>No hay cursos disponibles en este momento, existe un error al cargarlos</div>
            <p><a href="./bienvenida.html">Volver a Bienvenida</a></p>
        <% } %>
    </main>
</body>

</html>
